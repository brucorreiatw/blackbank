name: Deploy Front

on:
  push:
    branches:
    - main

jobs:
  deploy:
    runs-on: python
    steps:
    - name: Checkout
      uses: actions/checkout@v3

    - name: Run Tests
      run: make tests